<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shopMapper">
	
	<!-- 장바구니 -->
	<select id="cart" resultType="com.tr.VO.CartVO">
		SELECT 		cId, cNo, cName, cInfo, cPrice, cCount, (cPrice * cCount) as cTotal
		FROM			tr_cart
		WHERE		cId = #{cId}
	</select>
	
	<!-- 장바구니_상품삭제 -->
	<delete id="delete">
		DELETE FROM tr_cart
		WHERE cId = #{cId}
		AND	cNo = #{cNo}
	</delete>
</mapper>