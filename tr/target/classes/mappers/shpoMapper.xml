<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <!-- 다른 mapper와 중복되지 않도록 네임스페이스 기재 -->
 <mapper namespace="shop">
 
  	<!-- 상품 소감(댓글) 작성 -->
	<insert id="registReply">
	 insert into tbl_reply (
	 				pno, rwriter, rno, rcondtent)
	     values (
	     #{pNO}, #{rWriter}, sq_reply.nextval, 
	     #{rContent})
	</insert>
	
	<!-- 카데고리  호출-->
		<select id="list" resultType="com.tr.VO.ProductVO">
		select
		    p.pno, p.pName, p.pCode, c.cateName, pPrice, pImg, pInfo
		 from tr_pro p
		            inner join tr_category c
		                on p.pCode = c.cateCode           
		            where p.pCode = #{cateCode}
		</select>
		
 </mapper>